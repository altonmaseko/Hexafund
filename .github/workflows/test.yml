name: Example Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      CONNECTION_URI: 'your_connection_uri_here'
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Display CONNECTION_URI
        run: echo $CONNECTION_URI
